import { appTasks } from '@ohos/hvigor-ohos-plugin';

export default {
    system: appTasks,  /* Built-in plugin of Hvigor. It cannot be deleted. */
    plugins:[]         /* Custom plugin to obtain extended features. */
}