import { hapTasks } from '@ohos/hvigor-ohos-plugin';

export default {
    system: hapTasks,  /* Built-in plugin of Hvigor. It cannot be deleted. */
    plugins:[]         /* Custom plugin to obtain extended features. */
}